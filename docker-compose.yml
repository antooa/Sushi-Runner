version: '3'
services:
  db:
    image: 'mcr.microsoft.com/mssql/server:2017-latest'
    ports:
      - '1433:1433'
    environment:
      SA_PASSWORD: 'SushiRunner1'
      ACCEPT_EULA: 'Y'

  webapp:
    build:
      context: '.'
      dockerfile: Dockerfile
    ports:
      - '8080:8080'
    depends_on:
      - db
